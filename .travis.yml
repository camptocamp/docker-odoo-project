sudo: required

services: docker

env:
  matrix:
    - VERSION=11.0
    - VERSION=10.0
    - VERSION=9.0

script:
  - make VERSION=$VERSION build
  - make VERSION=$VERSION FULL=True build
  - make VERSION=$VERSION test

after_success:
  - travis/publish.sh
  - FULL=True bash -c 'travis/publish.sh'
